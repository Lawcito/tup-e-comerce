import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.clocknails.app',
  appName: 'Clock Nails',
  webDir: 'dist/e-comerce/browser',
};

export default config;
